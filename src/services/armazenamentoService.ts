export async function salvarImagemTemporaria(imagemBase64: string, guid: string): Promise<string> {
    // Implementar lógica para salvar a imagem temporariamente
    // Retornar o link temporário para a imagem
    return '';
  }